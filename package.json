{
  "name": "a4club-importador",
  "version": "1.0.0",
  "description": "A4 CLUB - Importador automatico Google Drive -> Cloudflare R2 + PostgreSQL",
  "main": "src/server.js",
  "scripts": {
    "postinstall": "npx prisma migrate deploy || true",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/lib-storage": "^3.600.0",
    "@prisma/client": "^5.16.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^140.0.0",
    "node-cron": "^3.0.3",
    "p-limit": "^3.1.0"
  },
  "devDependencies": {
    "prisma": "^5.16.0"
  }
}
